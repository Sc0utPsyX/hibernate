CREATE TABLE customers
(
    id   BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name VARCHAR(255),
    CONSTRAINT pk_customers PRIMARY KEY (id)
);
CREATE TABLE products
(
    id    BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    title VARCHAR(255),
    cost  DECIMAL,
    CONSTRAINT pk_products PRIMARY KEY (id)
);
CREATE TABLE SavedItem
(
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    customer_id BIGINT,
    product_id  BIGINT,
    price       DECIMAL,
    qty         INTEGER,
    CONSTRAINT pk_saveditem PRIMARY KEY (id)
);

ALTER TABLE SavedItem
    ADD CONSTRAINT FK_SAVEDITEM_ON_CUSTOMER FOREIGN KEY (customer_id) REFERENCES customers (id);

ALTER TABLE SavedItem
    ADD CONSTRAINT FK_SAVEDITEM_ON_PRODUCT FOREIGN KEY (product_id) REFERENCES products (id);